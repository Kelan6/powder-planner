import { ZonedDate } from '@progress/kendo-date-math';
/**
 * Returns a duration between start and end dates.
 *
 * @hidden
 */
export declare const duration: (start: ZonedDate, end: ZonedDate) => number;
