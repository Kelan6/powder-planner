import { SchedulerProps } from '../Scheduler';
/**
 * Represents the Object which is passed to the [`ref`](https://reactjs.org/docs/refs-and-the-dom.html) callback of the Scheduler component.
 */
export interface SchedulerHandle {
    props: SchedulerProps;
    element: HTMLDivElement | null;
}
