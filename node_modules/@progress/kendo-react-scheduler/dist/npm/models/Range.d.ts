import { ZonedDate } from '@progress/kendo-date-math';
/** @hidden */
export interface Range {
    index: number;
    start: Date;
    end: Date;
    zonedStart: ZonedDate;
    zonedEnd: ZonedDate;
    isAllDay?: boolean;
}
