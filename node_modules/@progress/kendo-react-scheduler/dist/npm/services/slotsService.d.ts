import { Group, Range, Slot, DateRange } from '../models';
/** @hidden */
export declare const toSlots: (_dateRange: DateRange, { step }: any, { groups, ranges }: {
    groups: Group[];
    ranges: Range[];
}) => Slot[];
